# Copy the values from https://console.upstash.com/redis/ REST API
# Locally these are read from .dev.vars by wrangler; in production set them
# with `wrangler secret put <NAME>` or in the Cloudflare dashboard.
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
